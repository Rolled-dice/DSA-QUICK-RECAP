In this we know the  current place index is sort we have to check for the i+1.. 
this one also took o(n^2) in worst case where we have the reverse sorted array......

vector<int> insertion_sort(int v){
   int n = v.size();
   for(int i=0;i<n;i++){
       for(int j=i;j>=0;j--){
           if(v[j]<v[j-1])swap(v[j],v[j-1]);
           else break;
       }
    }
return v;
}
